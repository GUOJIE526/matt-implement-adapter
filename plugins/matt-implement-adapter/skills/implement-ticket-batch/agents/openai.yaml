interface:
  display_name: "Matt Implement Batch Adapter"
  short_description: "Run approved tickets from a validated scheduler frontier"
  default_prompt: "Use $implement-ticket-batch for this approved multi-ticket batch: create the validated batch plan first, start only the scheduler-approved unblocked frontier, run parallel workers only within that frontier, integrate in dependency order, and report completion evidence."

policy:
  allow_implicit_invocation: true
